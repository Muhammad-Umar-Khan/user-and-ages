import "./User.css";
import UsersList from "../usersList/UsersList";
import Card from "../Card/Card";
const User = () => {
  return (
    <Card>
      <UsersList />
    </Card>
  );
};

export default User;
